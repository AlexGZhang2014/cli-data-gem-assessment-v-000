RSpec.describe Top::Ten::Video::Games do
  it "has a version number" do
    expect(Top::Ten::Video::Games::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
